
# Notas:
```
npm install 
```

Este es un programa en Nodejs

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Es la base de la tabla a multiplicar       [número] [requerido]
  -h, --limite   limite de la tabla a crear                             [número]
  -l, --listar   Muestra la tabla que se creó        [booleano] [defecto: false]
  ```
